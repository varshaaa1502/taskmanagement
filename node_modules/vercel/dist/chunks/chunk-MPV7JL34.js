import { createRequire as __createRequire } from 'node:module';
import { fileURLToPath as __fileURLToPath } from 'node:url';
import { dirname as __dirname_ } from 'node:path';
const require = __createRequire(import.meta.url);
const __filename = __fileURLToPath(import.meta.url);
const __dirname = __dirname_(__filename);
import{buildCommandWithGlobalFlags,quoteArg}from"./chunk-NS4FH2TY.js";import{output_manager_default}from"./chunk-Z6Y55QJW.js";import{require_source}from"./chunk-U7KCDTCH.js";import{__toESM}from"./chunk-6L236MNA.js";var import_chalk=__toESM(require_source(),1);function formatProjectScope(scope){return scope.type==="all"?"all projects":`${scope.projectName} (${scope.projectId})`}function formatNotificationExpanded(notification,index){if(notification.type==="slack")return[`    ${index+1}. ${import_chalk.default.bold("Slack")} ${import_chalk.default.cyan(`#${notification.channelName}`)}`,`       ${import_chalk.default.dim("Project scope:")} ${formatProjectScope(notification.projectScope)}`,`       ${import_chalk.default.dim("Channel ID:")} ${notification.channelId}`,`       ${import_chalk.default.dim("Slack installation ID:")} ${notification.configId}`].join(`
`);let{webhook}=notification;return[`    ${index+1}. ${import_chalk.default.bold("Webhook")}`,`       ${import_chalk.default.dim("Webhook ID:")} ${webhook.id}`,`       ${import_chalk.default.dim("URL:")} ${"url"in webhook?webhook.url:"Details unavailable"}`].join(`
`)}function formatRuleExpanded(ruleId,notifications){let lines=[`  ${import_chalk.default.bold(ruleId)}`];return notifications.length===0?lines.push(`    ${import_chalk.default.dim("No notification destinations")}`):lines.push(notifications.map((notification,index)=>formatNotificationExpanded(notification,index)).join(`

`)),lines.join(`
`)}function printRuleNotifications(ruleId,notifications){if(notifications.length===0){output_manager_default.log(`No notification destinations are linked to alert rule ${ruleId}.`);return}output_manager_default.print(`
${import_chalk.default.bold("Notification destinations")} for ${import_chalk.default.cyan(ruleId)}

${notifications.map(formatNotificationExpanded).join(`

`)}
`)}function printRulesNotifications(result){let ruleEntries=Object.entries(result.notifications).filter(([,notifications])=>notifications.length>0),rendered=ruleEntries.length>0?`
${import_chalk.default.bold("Rule notification destinations")}

${ruleEntries.map(([ruleId,notifications])=>formatRuleExpanded(ruleId,notifications)).join(`

`)}
`:`
No linked notification destinations found.
`;output_manager_default.print(rendered)}function formatTarget(target){return target.type==="webhook"?`webhook ${target.webhookId}`:`Slack channel ${target.channelId}${target.configId?` (installation ${target.configId})`:""}`}function buildRuleNotificationsInspectCommand(argv,ruleId){return buildCommandWithGlobalFlags(argv,`alerts rules notifications ${quoteArg(ruleId)}`)}function printRuleNotificationMutationReceipt(action,ruleId,target,argv){let preposition=action==="Linked"?"to":"from",inspectCommand=buildRuleNotificationsInspectCommand(argv,ruleId);output_manager_default.print(`
${import_chalk.default.green("\u2713")} ${action} ${formatTarget(target)} ${preposition} alert rule ${import_chalk.default.cyan(ruleId)}
${import_chalk.default.dim(`Inspect: ${inspectCommand}`)}
`)}export{printRuleNotifications,printRulesNotifications,buildRuleNotificationsInspectCommand,printRuleNotificationMutationReceipt};
